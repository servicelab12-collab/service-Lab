<p>Bonjour,</p>
<p>Pour réinitialiser votre mot de passe ServiceLab Partner Portal, cliquez sur le lien suivant :</p>
<p>
    <a href="{{ url('app_reset_password', {token: resetToken.token}) }}">
        {{ url('app_reset_password', {token: resetToken.token}) }}
    </a>
</p>
<p>Ce lien expire dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>
<p>Si vous n’avez pas demandé cette réinitialisation, ignorez cet e-mail.</p>
